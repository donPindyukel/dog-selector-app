import { MakeUpFirstLetterPipe } from './make-up-first-letter.pipe';

describe('MakeUpFirstLetterPipe', () => {
  it('create an instance', () => {
    const pipe = new MakeUpFirstLetterPipe();
    expect(pipe).toBeTruthy();
  });
});
